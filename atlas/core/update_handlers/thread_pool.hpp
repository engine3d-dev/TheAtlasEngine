#pragma once
#include <core/core.hpp>
#include <thread>

namespace atlas{

    /**
     * @note There are a few things to note when considering implementing the thread pool
     * 
     * 
    */

    class thread_pool {
    public:
        thread_pool(const uint32_t p_size);
    };
};