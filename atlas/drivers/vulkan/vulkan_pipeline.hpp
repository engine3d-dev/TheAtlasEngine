#pragma once
#include <string>

namespace atlas::vk{
    class VulkanPipeline{
    public:
        VulkanPipeline() = default;
        VulkanPipeline(const std::string& p_tag);

    private:
    
    };
};