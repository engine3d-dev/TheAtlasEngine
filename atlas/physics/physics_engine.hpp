#pragma once
#include <flecs.h>

namespace atlas {
    class physics_engine {
    public:
        physics_engine(flecs::world* p_registry);

        // void add_velocity(const glm::vec3& p_velocity, )
    private:
    };
};